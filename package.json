{
  "name": "retro_server",
  "version": "1.0.0",
  "description": "server HTTP pour API REST",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "server",
    "http",
    "api",
    "rest"
  ],
  "author": "maxime akouo",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
